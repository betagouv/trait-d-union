<div class="offres-card col mb-3 position-fixed mx-auto px-0">
  <div class="card mb-4 text-secondary">
    <div class="card-body">
      <h2 class="card-title">
        {{titre}}
      </h2>
      <div class="card-text text-left">
        <div class="text-wrap">
          <h6>{{lieuTravail}}</h6>
          <small>{{dureeTravail}}</small>
          <br>
          <div class="badge badge-info">{{sessionDuration}}</div>
          <div class="badge badge-info">et ensuite 👉</div>
          <div class="badge badge-info">{{typeContrat}}</div>
          <div class="badge badge-info">{{salaire}}</div>
        </div>
        <br>
        {{description}}
      </div>
    </div>
  </div>
</div>
